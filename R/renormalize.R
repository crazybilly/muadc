renormalize  <- function {
  
  # turn this into a bearable function
#   df <- read.csv('that-annoying-list.csv')
#   activities <- strsplit(df$activities, "~")
#   normalizedlist <- data.frame(user = rep(df$id, sapply(activities, length)), item = unlist(activities))
#   
}